# Architecture

## Active authority

The project is now governed by the post-Survey V2 planning baseline accepted on **2026-08-11**.

Authority order:

1. `docs/product/PRODUCT_CONSTITUTION_V1.0.md` — product-level authority.
2. `ARCHITECTURE_CONTRACT_V0.2.md` — **ACTIVE architecture baseline**.
3. `docs/capabilities/` — active capability specification set.
4. `docs/adr/` — architecture decision records.
5. implementation / provider behavior.

The explicit acceptance record is:

`docs/roadmap/A0_PLANNING_BASELINE_ACCEPTANCE.md`

The `CANDIDATE` wording inside the original v0.2 draft header records its pre-acceptance drafting state. The A0 acceptance record supersedes that status label without rewriting the historical draft text.

## Historical contracts

The v0.1.x series remains preserved for provenance:

- `ARCHITECTURE_CONTRACT_V0.1.md`
- `ARCHITECTURE_CONTRACT_V0.1.1.md`
- `ARCHITECTURE_CONTRACT_V0.1.2.md`

When a v0.1.x rule conflicts with Product Constitution v1.0 or accepted Architecture Contract v0.2, it is **historical only** and must not be implemented as current behavior.

In particular, historical remote/generated visual fallback semantics are not active product behavior.

## Current construction phase

The active roadmap phase is:

`R0.7A — Architecture v0.2 Migration Foundation`

Migration audit:

`docs/roadmap/R0.7A_MIGRATION_AUDIT.md`

Core authority chain:

`Brief -> ScriptPlan -> ShootingPlan -> user visual Assets / Shots -> evidence -> BeatMap -> EditPlan -> ResolutionDecision -> spatial/audio decisions -> EDL -> Render -> layered Review`

External models, providers and open-source projects contribute capability implementations only. They never override Domain ownership or Product Constitution policy.
