# R0.11 Final Closure — Spatial Composition / Auto Reframe

**Closed:** 2026-08-14  
**Classification:** `PASS_WITH_MINOR_DEFECT`  
**Accepted code baseline:** `d06592560dbeb764666592effa00f7d5537715ef`

## Scope closed

R0.11 established a provider-neutral Auto Reframe path where:

- spatial evidence providers observe/track only;
- `SpatialComposer` owns executable reframe decisions;
- `SpatialTransformPlan` is canonical execution truth;
- tracked plans carry explicit `LINEAR` interpolation while static/manual plans may use `HOLD`;
- FFmpeg executes canonical crop paths without inventing editorial smoothing;
- lost observations contain no fabricated geometry;
- short terminal loss and bounded reacquisition are distinct policies;
- manual locks remain higher authority than automatic solve.

## Accepted technical evidence

Final interpolation-aware QC:

- Movement RAW: containment `40/40`, 41 keyframes, max path velocity 240 px/s.
- Movement STABILIZED: containment `40/40`, 14 keyframes, max path velocity 195 px/s.
- Occlusion RAW: containment `96/96`, 99 keyframes, max path velocity 600 px/s.
- Occlusion STABILIZED: containment `96/96`, 14 keyframes, max path velocity 450 px/s.
- Main occlusion recovery: `47/30 → 133/30`, about 2.8667 s.
- Recovered identity: same intended seeded subject.
- No interpolation-aware containment failures.
- No source/aspect violations.
- No unresolved final Product Probe plans.

`SpatialTransformPlan.evaluate_crop()` is the canonical HOLD/LINEAR source-time evaluator used by QC semantics; the FFmpeg v2 adapter preserves the same deterministic exact-rational/round-half-up meaning.

Accepted Quality Gate for code baseline:

- Ruff: PASS;
- mypy: PASS;
- pytest: `487 passed`;
- import contracts: PASS;
- build: PASS;
- diff check: PASS;
- remote `ci/quality-gate-diagnostic`: success.

## Human Gate

Rights-attested local Product Probe previews were compared against static center-crop baselines.

Movement/no intentional occlusion:

- preferred: `stabilized`;
- feel: natural;
- subject framing: normal.

Occlusion/recovery:

- preferred: `stabilized`;
- recovery: accepted;
- visible note: minor micro-jump around recovery.

The minor recovery jump is recorded as a known product limitation. It is not a closure blocker and should not trigger further parameter tuning without broader real-media evidence showing that it is systematic/material.

## Provider / release status

Current recovery candidate:

- MediaPipe 0.10.31 runtime, optional;
- external EfficientDet Lite0 model;
- expected SHA-256 `0720BF247BD76E6594EA28FA9C6F7C5242BE774818997DBBEFFC4DA460C723BB`;
- model file is not committed/bundled.

Exact redistribution/commercial terms for that downloaded model artifact remain:

`RELEASE_LICENSE_PENDING`

This does not reopen R0.11 engineering/product closure. A future distribution/bundling path must resolve the artifact terms or fail closed/use another authorized acquisition model.

## Deferred notes

- recovery-window candidate (`<=4 s`) remains Product-Probe calibrated, not constitutional truth;
- minor recovery micro-jump remains a benchmark note;
- broader multi-subject/impossible-crop corpus remains future quality expansion, not required to reopen this phase absent new evidence.

## Next

Before substantive R0.12 product work, execute the bounded control-plane compression work order `CONTROL-PLANE-001`, then proceed to R0.12 EDL / Renderer / Subtitle / Preview / Proxy Productization.
