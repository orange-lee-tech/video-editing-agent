# R0.12 Preview Real Playback Benchmark Evidence

**Work Order:** `R0.12-PREVIEW-BACKEND-BENCHMARK-001`  
**Date:** 2026-08-16  
**Status:** IN_PROGRESS  
**Environment:** Class-A restored-vendor-driver — Lenovo ThinkPad T470s / i5-6300U / Intel HD Graphics 520 `27.20.100.8854`, Oray virtual display still present.

## Benchmark corpus — Wave 1

Deterministic local fixture generated by the project-owned FFmpeg runtime:

- MP4 container;
- H.264 Constrained Baseline;
- 1920x1080, 30 fps;
- AAC-LC 48 kHz mono;
- duration approximately 12 seconds.

This wave tests actual windowed playback/presentation and resource behavior. It does **not** yet provide the final deterministic seek/scrub latency comparison.

## Runtime isolation

Observed before Wave 1:

- `gst-play-1.0`, `gst-launch-1.0`, `gst-inspect-1.0`, and `vlc` are not available from global PATH;
- benchmark root is not present in User PATH or Machine PATH;
- both candidates run from private benchmark directories.

This supports the private/side-by-side runtime direction and avoids credit for preconfigured developer-machine state.

## GStreamer 1.28.6 — Wave 1

Command path used the private GStreamer runtime and explicitly selected `d3d11videosink` for actual windowed playback.

Observed process-level measurement:

- wall time: approximately `13.683s`;
- first observed window handle: approximately `518 ms` after process start;
- CPU time: approximately `5.609s`;
- average machine CPU estimate: approximately `10.2%` on this 4-logical-processor host;
- max working set: approximately `139.7 MiB`.

Diagnostic evidence:

- Direct3D11 devices were created/enumerated during actual playback;
- D3D11 presentation window was created and later destroyed normally;
- the runtime reports unsupported VP8/VP9/AV1 decode capability for some enumerated D3D11 devices on this legacy host;
- one enumerated D3D11 device reported `0x80004002` / unsupported interface when queried for video-device capability;
- despite that, actual playback completed and the visible D3D11 presentation window was destroyed at normal end of playback.

Interpretation at Wave 1:

- actual D3D11 presentation path is active;
- hardware H.264 decoder registration was proven in Stage 1, but the Wave-1 excerpt alone did not prove beyond ambiguity that decodebin selected `d3d11h264dec`;
- the device-enumeration warning is retained as diagnosable degraded-environment evidence and is not hidden by removing Oray before the benchmark;
- the missing D3D11/DXGI debug libraries are developer debug-layer diagnostics, not a playback failure.

## VLC/libVLC 3.0.23 — Wave 1

Observed process-level measurement:

- wall time: approximately `13.392s`;
- first observed window handle: approximately `838 ms` after process start;
- CPU time: approximately `3.188s`;
- average machine CPU estimate: approximately `6.0%`;
- max working set: approximately `291 MiB`.

Diagnostic evidence explicitly proves:

- H.264 handled through avcodec;
- D3D11-capable output formats were discovered;
- VLC selected `d3d11va_vld`;
- Direct3D11 device creation succeeded;
- H.264 D3D11 decoder surfaces were created successfully;
- hardware decoder module `d3d11va` was selected;
- log states: `Using D3D11VA (Intel(R) HD Graphics 520, vendor 8086(Intel), device 1916, revision 7) for hardware decoding`.

A late `SetThumbNailClip failed: 0x800706f4` line occurred during shutdown and did not prevent successful full playback. It remains diagnostic evidence to revisit if it appears during interactive seek/scrub or embedding.

## Wave-1 comparison — provisional only

On this one Class-A restored-vendor-driver host and this one deterministic 1080p H.264/AAC fixture:

- both candidate families completed actual windowed playback;
- GStreamer showed an earlier first-observed window (`~518 ms` vs `~838 ms`);
- GStreamer showed lower peak working set (`~140 MiB` vs `~291 MiB`);
- VLC showed lower measured CPU use in this run (`~6.0%` vs `~10.2%` machine estimate);
- no backend winner is declared from these values.

The `FirstWindowMs` metric is a window-handle appearance proxy and must not be represented as exact first-frame latency.

## Wave 2 — GStreamer actual playbin3 hardware path proof — PASS

After retiring two brittle manual `filesrc/qtdemux` proof pipelines and correcting a Windows PowerShell 5.1 native-stderr harness defect, the same deterministic fixture was run through the actual high-level GStreamer playback stack using:

- canonical `file:///` URI input;
- `playbin3` / `uridecodebin3` / `decodebin3`;
- `GST_PLUGIN_FEATURE_RANK=d3d11h264dec:MAX`;
- explicit `d3d11videosink`;
- `Start-Process` with separated stdout/stderr;
- `GST_DEBUG_DUMP_DOT_DIR` pipeline topology capture.

Observed:

- playback process exit code: `0`;
- five DOT graphs captured across NULL→READY→PAUSED→PLAYING→PAUSED→READY state changes;
- `GstD3D11H264Dec:d3d11h264dec0` present in the actual autoplugged `decodebin3` graph;
- `GstD3D11VideoSink:d3d11videosink0` present in the actual `playsink` graph;
- decoder context reports hardware device `Intel(R) HD Graphics 520`, vendor `8086`, device `1916`, `hardware=true`;
- H.264 constrained-baseline 1920x1080@30 input reaches `d3d11h264dec0`;
- decoder output is `video/x-raw(memory:D3D11Memory)`, NV12, 1920x1080@30;
- D3D11-memory NV12 remains present through the downstream playback graph into `d3d11videosink0`.

Interpretation:

- GStreamer hardware H.264 decode selection is now directly proven on this host through the actual playback stack, not merely through decoder registration;
- actual decode→presentation path is `playbin3 → decodebin3 → d3d11h264dec → D3D11Memory/NV12 → d3d11videosink`;
- the prior manual-pipeline failures remain benchmark harness defects and do not reduce candidate acceptance;
- the GStreamer hardware-path proof gate is closed and must not be reopened by further manual demux experiments.

## Harness defects discovered and retired

1. The first Wave-2 manual `filesrc -> qtdemux -> ...` proof pipeline was structurally brittle and failed before a meaningful decoder result.
2. A second manually linked `qtdemux` attempt again failed at the source-start boundary despite the same fixture already succeeding through `gst-play-1.0`.
3. Windows PowerShell 5.1 treated GStreamer DEBUG messages written to native stderr as PowerShell errors under `$ErrorActionPreference = "Stop"`, prematurely aborting an otherwise valid playbin3 run.
4. Native benchmark execution therefore uses `Start-Process` + redirected stdout/stderr + child exit code for the remainder of this work.

Durable correction details:

`docs/validation/R0.12_PREVIEW_WAVE2_HARNESS_CORRECTION.md`

## Next benchmark gate — API control / seek / scrub

Hardware-path proof is no longer the blocker. Before winner selection:

1. exercise GStreamer through the official `GstPlay` control API;
2. exercise VLC through the libVLC 3.0 control API;
3. verify pause/resume behavior;
4. execute the same deterministic sequence of repeated absolute seeks;
5. measure a clearly named seek-recovery proxy without presenting it as exact photon-to-screen latency;
6. observe process stability and recovery after repeated control operations;
7. then test representative real/VFR footage and explicit software-fallback behavior;
8. keep Class-B/Class-C evidence gap explicit;
9. keep libmpv on its separate auditable LGPL build/provenance gate.

## Architecture interpretation

Nothing in these playback results changes authority boundaries:

- Preview remains playback-only;
- EDL remains sole exact timeline authority;
- final render remains canonical EDL → Renderer;
- benchmark results may select an adapter/runtime family but may not transfer editing authority into the playback library.
