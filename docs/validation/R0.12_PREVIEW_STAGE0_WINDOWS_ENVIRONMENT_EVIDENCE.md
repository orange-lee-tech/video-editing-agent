# R0.12 Preview Stage 0 — Windows Environment Evidence

**Work Order:** `R0.12-PREVIEW-BACKEND-BENCHMARK-001`  
**Date:** 2026-08-16  
**Classification:** CLASS_A_DEGRADED_FALLBACK + CLASS_A_RESTORED_VENDOR_DRIVER  
**Purpose:** Durable environment evidence for Preview backend benchmarking. This is not a backend selection or production-runtime approval.

## Observed host

- Windows 11 Home, build family 26100, x64.
- CPU: Intel Core i5-6300U (2 cores / 4 logical processors).
- RAM: approximately 19.88 GiB.
- Physical Intel display hardware: `PCI\\VEN_8086&DEV_1916&SUBSYS_224B17AA`.
- Virtual display device present: `OrayIddDriver Device`, version `17.50.19.949`.

The same physical host has now been observed in two useful states:

1. **Class-A degraded/fallback state** — physical adapter bound to `Microsoft Basic Display Adapter`, version `10.0.26100.1`;
2. **Class-A restored-vendor-driver state** — physical adapter bound to `Intel(R) HD Graphics 520`, driver `27.20.100.8854`.

This machine remains a legacy/low-end evidence host. Successful restoration does not promote it to the ordinary supported Class-B hardware definition.

## Project media runtime

The project-controlled FFmpeg tools are present under:

`.tools/ffmpeg-8.1/ffmpeg-8.1-full_build/bin/`

Observed build:

- FFmpeg 8.1 full build from gyan.dev;
- GPL/version3 enabled;
- D3D11VA/D3D12VA/DXVA2/QSV/VPL/NVDEC/NVENC and other acceleration paths compiled in.

This build is valid engineering evidence for current development probes. It is **not automatically an approved commercial redistribution profile**; R0.14 must freeze exact FFmpeg build flags, license/notices and packaging provenance separately.

## Preview candidates currently installed

None observed on PATH at Stage 0:

- `gst-launch-1.0`: not found;
- `gst-inspect-1.0`: not found;
- `mpv`: not found;
- `vlc`: not found.

## Toolchain

- Python 3.13.14 observed globally.
- uv 0.12.3.
- winget available; scoop/choco absent.

The repository/product must not infer that a global Python, FFmpeg or Preview runtime is preinstalled for ordinary users.

## Stage 0.5 — Driver Store / platform gate

Additional local evidence before restoration:

- machine manufacturer/model: `LENOVO 20JTS1R400` (ThinkPad T470s, type 20JT);
- BIOS: `N1WET48W (1.27)`, dated 2018-05-28;
- the physical display device was started and bound to `display.inf` / Microsoft Basic Display Adapter;
- `pnputil /enum-devices ... /drivers` reported no Intel matching driver package for the device;
- `pnputil /enum-drivers /class Display` reported only the Oray virtual-display package `oem29.inf`;
- therefore there was no signed Intel graphics package in the local Driver Store that could simply be rebound/restored.

Official-source interpretation used for the restoration decision:

- Lenovo publishes a T470s 20JS/20JT-specific Intel graphics driver path, although the OEM package is a Windows 10-era package;
- Intel publishes `31.0.101.2115` for Intel HD Graphics 520 / 6th-generation graphics and exposes a Windows 11-family download, while also marking 6th-generation graphics EOL;
- therefore the benchmark preferred the machine-specific Lenovo OEM package first and reserved the generic Intel package only as a fallback experiment.

## Stage 0.6 — Successful OEM graphics restoration

The Lenovo OEM path installed successfully on the current Windows 11 host without a forced INF/Have-Disk path.

Observed after reboot:

- physical adapter: `Intel(R) HD Graphics 520`;
- hardware ID remains `PCI\\VEN_8086&DEV_1916&SUBSYS_224B17AA`;
- driver provider: `Intel Corporation`;
- driver version: `27.20.100.8854`;
- driver date: `2020-10-14`;
- Driver Store package: `oem34.inf`, original `igdlh64.inf`;
- device status: `OK`;
- the Oray virtual display remains installed and healthy as `oem29.inf`, version `17.50.19.949`.

Interpretation:

- the physical GPU is no longer in the Microsoft Basic Display fallback state;
- the same host can now provide useful vendor-driver D3D11/DXVA evidence while retaining its earlier degraded-state record;
- this remains **Class-A restored-vendor-driver evidence**, not proof of Class-B/default-user performance;
- Oray remains in place for now. It should only be disabled temporarily if later adapter-selection evidence shows concrete interference.

## Next gate

Before installing Preview candidates:

1. verify the project FFmpeg runtime can enumerate/init Windows hardware paths with the restored Intel driver;
2. run one deterministic software-decode baseline and one D3D11VA hardware-decode probe on the same fixture;
3. optionally probe QSV only as secondary capability evidence — QSV success is not a requirement for Preview backend eligibility;
4. record fallback behavior and any adapter-selection ambiguity caused by the virtual display;
5. only after the device/capability probe is understood proceed to controlled GStreamer/libVLC installation and the separately governed LGPL-libmpv provenance path.
