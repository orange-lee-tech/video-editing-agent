# R0.12 Preview Stage 1 — Candidate Preparation Evidence

**Work Order:** `R0.12-PREVIEW-BACKEND-BENCHMARK-001`  
**Date:** 2026-08-16  
**Status:** IN_PROGRESS  
**Scope:** Official candidate provenance / private-runtime preparation only; no backend selection yet.

## Entry condition

Stage 0 is accepted as sufficient for candidate preparation:

- Lenovo ThinkPad T470s / Intel HD Graphics 520 restored to Lenovo OEM driver `27.20.100.8854`;
- software decode fallback PASS;
- D3D11 device initialization PASS;
- H.264 D3D11VA decode PASS;
- Oray virtual display remains installed and did not prevent FFmpeg adapter selection in the capability probe.

## Candidate families

- GStreamer 1.28.x official Windows MSVC x86_64 distribution;
- VLC/libVLC 3.0.23 official Windows x86_64 distribution;
- libmpv remains separately gated because an accepted product path requires auditable LGPL configuration/provenance rather than an arbitrary common Windows binary.

## First preparation attempt — INVALID / NOT ACCEPTED

The first PowerShell preparation script must not be used as evidence that either candidate failed.

Observed problems were in the benchmark preparation script / HTTP handling, not in GStreamer or VLC:

1. GStreamer's `.sha256sum` response was exposed to Windows PowerShell 5.1 as `System.Byte[]`; calling `.Trim()` therefore failed and left the expected hash empty.
2. The subsequent interactive commands were still executed manually after a `throw`, so later output misleadingly printed `checksum PASS` even though the prior command had failed. The corrected procedure must be one fail-closed script unit.
3. The VLC `get.videolan.org/...zip` endpoint returned the VideoLAN download landing HTML under `Invoke-WebRequest` rather than the binary ZIP payload. The downloaded file hash therefore did not match the official ZIP checksum and `Expand-Archive` correctly rejected it.
4. The VLC `.sha256` request through the same download frontend likewise returned HTML (`<!doctype...`) rather than the raw checksum file.
5. GStreamer installation was attempted only after the invalid checksum stage and did not produce the requested private runtime. This result is not classified as a GStreamer install defect until the package is revalidated and the installer is run from a fail-closed script.

No candidate is eliminated by this attempt.

## Official-source facts for retry

### GStreamer

Current official download page reports stable `1.28.6` and recommends MSVC x86_64 when unsure. Starting with 1.28, Windows installers use Inno Setup and officially accept:

- `/DIR=path`;
- `/TYPE=runtime`;
- `/CURRENTUSER`;
- `/SILENT` / `/VERYSILENT`;
- `/NORESTART`.

The retry must save the checksum response as bytes/file and decode it explicitly rather than assuming PowerShell returns a text string.

### VLC/libVLC

VideoLAN's official 3.0.23 win64 directory contains `vlc-3.0.23-win64.zip` with expected size `79,893,405` bytes.

VideoLAN's download page reports the ZIP SHA-256 as:

`992d19dbd0b8a7cde9167d2f7780b1ef6f92acc8a71acfa736101a21f35181e1`

The retry must use the static official download directory rather than treating the `get.videolan.org` landing page as a direct binary endpoint. It must verify both expected size/magic (`PK`) and SHA-256 before extraction.

## Corrected retry — partial acceptance

The corrected fail-closed script produced valid evidence.

### GStreamer 1.28.6 — PREPARATION PASS

Observed:

- official expected SHA-256: `059251444d1267b486eba390b18d25fed87e10315e72f757ec6c7e912fa746b5`;
- downloaded installer SHA-256 matched exactly;
- PE/MZ validation PASS;
- installer Authenticode status reported `NotSigned`; this is a packaging/provenance fact to retain, not a runtime failure, because package bytes were independently verified against the official sidecar SHA-256;
- private current-user runtime installation exit code `0`;
- `gst-launch-1.0` reports GStreamer `1.28.6`;
- `d3d11` plugin present and reports LGPL;
- `d3d11videosink` present;
- `d3d11h264dec` present with rank `primary + 1`;
- hardware decoder identifies `Intel(R) HD Graphics 520`;
- decoder source caps include H.264 constrained-baseline/baseline/main/high family;
- D3D11 output path exposes `video/x-raw(memory:D3D11Memory)` with NV12;
- `d3d11h264dec` inspection exit code `0`.

Interpretation:

- GStreamer has passed the Stage-1 private-runtime/provenance/capability preparation gate on this Class-A restored-vendor-driver host;
- this is not yet a playback/seek/scrub benchmark result;
- the unsigned installer must remain an explicit R0.14 packaging consideration even though official SHA-256 verification succeeded.

### VLC/libVLC 3.0.23 — PACKAGE/EXTRACTION PASS, CLI SMOKE PENDING

Observed:

- downloaded static official ZIP size exactly `79,893,405` bytes;
- expected SHA-256 `992d19dbd0b8a7cde9167d2f7780b1ef6f92acc8a71acfa736101a21f35181e1`;
- actual SHA-256 matched exactly;
- ZIP magic validation PASS;
- archive extraction completed before the CLI version probe;
- the CLI smoke then emitted `cannot open config file (...\\AppData\\Roaming\\vlc\\vlcrc)` and the fail-closed wrapper stopped the remaining steps.

This is **not** classified as a VLC package/runtime failure yet. The package bytes and archive extraction are accepted. The remaining question is whether the private VLC/libVLC runtime starts cleanly when user config loading is intentionally disabled for benchmark isolation.

Official VideoLAN source history documents the `--ignore-config` option specifically to prevent loading/saving the VLC config file and notes its usefulness for libVLC-based applications. The next retry therefore uses `--ignore-config` and does not redownload either candidate.

## Current acceptance state

- GStreamer package provenance: **PASS**;
- GStreamer private runtime: **PASS**;
- GStreamer D3D11 sink/hardware-decoder registration: **PASS**;
- VLC package provenance: **PASS**;
- VLC archive extraction: **PASS**;
- VLC/libVLC isolated startup smoke: **PENDING**;
- libmpv LGPL provenance/build gate: **PENDING**;
- real playback/seek/scrub benchmark: **NOT STARTED**.

## Retry acceptance gate

Candidate preparation is accepted only when:

- package bytes are from the reviewed official distribution path;
- checksum parsing is deterministic under Windows PowerShell 5.1;
- checksum matches before any install/extract action;
- GStreamer private runtime contains `gst-launch-1.0.exe` and `gst-inspect-1.0.exe` and exposes the expected D3D11 plugin/sink surface;
- VLC private runtime contains `vlc.exe`, `libvlc.dll`, and its plugin tree;
- VLC/libVLC starts in an isolated no-user-config mode suitable for benchmark automation;
- global `PATH` remains unchanged for candidate commands;
- failures stop the entire script rather than allowing later steps to continue and create false PASS messages.

## Interpretation

The first failed preparation attempt remains useful product evidence: installer/download automation itself must be diagnosable and fail closed. Stage-A/R0.14 packaging cannot assume browser-style redirects, text response types, or globally installed runtimes.

The corrected retry additionally shows that private-runtime preparation is viable for both candidate families so far: GStreamer is fully prepared for playback benchmarking, while VLC only needs a bounded isolated-startup smoke before entering the same benchmark stage.