# R0.12 Stage-A Planning Product Gate

**Result:** PASS  
**Date:** 2026-08-18  
**Accepted production-code baseline:** `b6572602c0f7faaa22383dab9fffa361fb946e75`  
**Exact-head CI:** `32127020333` — `ci/quality-gate-diagnostic = success`

## Gate target

Stage-A Planning requires a real ordinary Windows user to provide a real planning target through the product launcher and obtain an inspectable ScriptPlan plus an executable ShootingPlan without repository editing or hand-authoring internal artifacts.

## Real Product Probe evidence

The ordinary `video-editing-agent launch` surface was used on the real Windows target with:

- a real user-created project;
- real title/objective/audience/platform/core-message inputs;
- a user-selected local reference video on the successful reference-assisted probe;
- the product Settings surface for user-owned reasoning/direction and visual-understanding credentials;
- no repository-file editing or hand-authored ScriptPlan/ShootingPlan.

After evidence-backed provider/runtime repairs, the real Planning flow completed through:

`project_ready → input_validation → ingest_understanding → planning_generation ScriptPlan → planning_generation ShootingPlan → completed`.

The launcher returned and presented exact persisted revisions:

- ScriptPlan `scp_b0f9fb1f00da457ea3fd80231e7c5ea0@1`;
- ShootingPlan `shp_bd97a72705db4a1f880bfe564e598870@1`.

The successful plan contained a hook/body/closing script structure, explicit spoken/visual guidance and timing, plus a shooting plan with phone resource guidance, per-requirement capture instructions and framing/motion guidance.

No API secret values are recorded in this evidence.

## Human Gate

The user explicitly judged:

- **Planning Human Gate:** acceptable / PASS;
- **ScriptPlan:** acceptable — the brevity was consistent with the deliberately brief user input; richer input is expected to permit richer output;
- **ShootingPlan:** acceptable — dialogue-led visual guidance is useful, while future UX/detail work should make even simple shooting instructions more explicitly line-by-line and time-aware;
- **obvious blocking problem:** none in the accepted successful result.

Therefore the Stage-A Planning Product Gate is **PASS**.

## Known non-blocking observations / follow-up

The PASS does not claim the Planning surface is feature-complete. Follow-up product work identified during the same ordinary-user session includes:

- localized generated output, progress text and product-facing error explanations;
- scroll/export ergonomics for long output;
- safe persistence of form profiles and API credential references;
- placeholder guidance for required/optional fields;
- more robust unsupported-claim repair when authoritative facts/reference inputs are absent;
- bounded reference-share-link handling;
- opt-in public-material guidance and similar-example research subject to source/rights invariants;
- startup splash/progress polish.

A separate real Planning input also caused the model proposal to assert unsupported concrete claims (for example natural-origin/purity implications) while `authoritative_facts` was empty. The semantic reviewer correctly rejected unsupported factual claims, but the ordinary product path currently terminates instead of automatically repairing the proposal into a safe creative version. This is recorded as a robustness/UX follow-up and does not invalidate the witnessed successful Planning Product/Human Gate.

## Preserved invariants

- Planning remains independently usable and may end at ScriptPlan/ShootingPlan.
- Reference media remains analysis-only and Resolver-ineligible.
- No unsupported factual claim is accepted merely because it appears in model output.
- Exact internal media/timeline authority remains unchanged.
- No Product/Human PASS is inferred from tests alone; this PASS is based on the real ordinary-user run and explicit user judgment.
