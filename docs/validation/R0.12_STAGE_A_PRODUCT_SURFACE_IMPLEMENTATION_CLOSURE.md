# R0.12 Stage-A Product Surface Implementation Closure

**Boundary:** `R0.12-STAGE-A-PRODUCT-GATE-CLOSURE-001`  
**Engineering implementation result:** PASS / ACCEPTED  
**Product Gate / Human Gate:** OPEN  
**Structural progress:** 90%

## Accepted implementation

The ordinary-user Stage-A product-surface implementation is accepted at exact production-code baseline:

`0134d0c4a741eb2babed7275c0aaef42045f2dc4`

Primary implementation commit:

`c765d4095f5337e1dc30ba2ef11308cc425d904e` — `feat: add Stage A product launcher`

Evidence-backed repair commit:

`0134d0c4a741eb2babed7275c0aaef42045f2dc4` — `fix: complete safe combined launcher path`

## Independent ChatGPT review

ChatGPT independently reobserved the exact pushed commits, changed-file boundaries and exact-head CI rather than accepting the Codex report as authority.

Observed exact-head CI:

- `c765d4095f5337e1dc30ba2ef11308cc425d904e` — `ci/quality-gate-diagnostic = success`, run `32109997581`;
- `0134d0c4a741eb2babed7275c0aaef42045f2dc4` — `ci/quality-gate-diagnostic = success`, run `32111192942`.

The repair was bounded to four files and closed the concrete review findings without reopening Resolver, EDL, Renderer, Preview or persistence architecture.

## Accepted product-surface capabilities

The accepted implementation now provides:

- `video-editing-agent launch` using stdlib Tkinter;
- ordinary Planning inputs without hand-written JSON or internal entity IDs;
- authoritative facts plus optional direct-HTTPS/local reference video;
- reference media retained as `REFERENCE_ANALYSIS_ONLY` and Resolver-ineligible;
- exact `ReferenceStyleGuidance` forwarded to both Script and Shooting owners;
- Planning without reference remaining independent of FFmpeg/visual/TransNet capability;
- live observation of existing `ProductFlowEvent` events without decision authority;
- reviewed FFmpeg/ffprobe discovery and TransNetV2 package-owned runtime/weights discovery;
- Environment Doctor Shot-detection readiness reporting;
- deterministic file/folder expansion with original media protected;
- readable exact ScriptPlan/ShootingPlan presentation;
- Editing final-MP4 / Review-correction presentation;
- Editing-only as the default valid path;
- optional Combined enrichment from exact Planning results from the same launcher session and same project, with no user-entered internal IDs;
- fail-closed ordinary-user validation for blank project/output paths and non-MP4 Editing destinations.

## Local verification report

Codex reported the final repair with:

- focused repair tests: 23 PASS;
- full pytest suite: 684 PASS;
- Ruff format/check: PASS;
- mypy: PASS;
- import-linter: PASS;
- `uv build`: PASS;
- `git diff --check`: PASS;
- Tkinter smoke: `TK_OK`;
- bounded launcher smoke: exit 0;
- final working tree clean and synchronized with `origin/main`.

These local claims are supporting evidence; exact pushed code and CI were independently reobserved before acceptance.

## What this closure does not prove

This closure is **not** Planning Product Gate PASS, Editing Product Gate PASS, Human Gate PASS or Stage-A 100%.

The target Windows host still needs the real runtime/provider capabilities required by the chosen probe path. The final gates require real user conditions through the ordinary launcher:

1. real Planning run and Human judgment of ScriptPlan/ShootingPlan usefulness and shootability;
2. real Editing run over user-selected local footage to an actual final MP4, followed by Human judgment;
3. Planning-only, Editing-only and Combined invariants preserved;
4. originals protected and Review/grounding/EDL authority unchanged.

Only after both core Product + Human Gates and the global Stage-A completion gate PASS may structural progress move from 90% to 100%.
