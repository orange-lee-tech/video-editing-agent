# R0.12 Windows Setup.exe Release Candidate Evidence

**Status:** SUPERSEDED FOR FINAL ACCEPTANCE — HISTORICAL ENGINEERING PASS  
**Release candidate source SHA:** `7753e5bbee93ca743152a7e2319c3f6739faff60`  
**Application version:** `0.1.0`  
**Windows RC workflow:** run #5 / `33140342038`  
**Setup.exe SHA-256:** `9ba68f361f2d4c7881e1192b82e2fb3d750332d8844796829224a9dd1912033e`  
**Artifact:** `VideoEditingAgent-Setup-7753e5bbee93ca743152a7e2319c3f6739faff60`

> This file remains durable historical engineering evidence. It is **not** the final Stage-A acceptance installer because the subsequent ordinary-user Human Gate exposed material desktop/packaging defects that were repaired after this source. The replacement installer must be built from the current accepted source candidate recorded in `CURRENT_CONTROL_STATE.md`.

## Automated release evidence

This RC workflow completed successfully on Windows and proved:

- exact source checkout and pinned CPython 3.12.13 packaging environment;
- Windows onedir build and static package inspection;
- packaged Doctor and runtime probe;
- packaged launcher smoke with an external Workspace;
- verified Inno Setup 7.1.0 immutable release asset, SHA-256 and valid Pyrsys B.V. Authenticode signature;
- guided `VideoEditingAgent-Setup-0.1.0.exe` compilation;
- Planning-only install with Editing runtime absent;
- Planning-only installed launcher smoke;
- Planning-only → Full upgrade with FFmpeg and reviewed TransNetV2 weights present;
- Full installed launcher smoke;
- same-version Full repair;
- deferred 2.0 speech runtime/model absent;
- uninstall removes the application while preserving the external Project Workspace.

The staged application contained 20,251 files and 1,300,783,310 bytes before installer compression. The final Setup.exe is 287,556,353 bytes.

## Why this RC was superseded

Ordinary-user desktop testing after this automated pass identified defects that could not be waived as cosmetic. Post-RC repairs include windowed GUI/diagnostics separation, installer pre-initialization repair, improved packaged GUI smoke semantics and additional ordinary-user UI/result handling.

Therefore this RC proves that the release-engineering mechanism worked for its source, but it cannot prove the final repaired source.

## 1.0 component boundary

Default Full installation contains:

- Core App / Planning;
- Media Runtime (FFmpeg/ffprobe);
- Scene Detection Runtime (TransNet/CPU Torch/reviewed weights).

Planning-only installation omits the Editing runtimes.

The following remain deferred from 1.0:

- speech runtime/model;
- sentence-preserving source-speech reconstruction;
- translated/bilingual subtitles;
- cross-language narration/TTS.

## Replacement evidence required

A new `Windows Release Candidate` workflow must run against the current accepted source candidate and produce a new SHA-addressed Setup.exe artifact plus lifecycle evidence.

Only that replacement installer may be used for the final ordinary-user Human Gate and Stage-A closure.

## Release-tool licensing note

This RC used Inno Setup 7.1.0. Its commercial-use licensing policy must be resolved before commercial distribution. This does not invalidate the historical engineering evidence.
