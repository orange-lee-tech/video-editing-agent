# R0.7A-1 Compatibility Primitives — Validation Record

**Status:** COMPLETE  
**Validated commit:** `22ff30bbfcc0a5d36aeb73ae3b4aa7d0530a1c51`  
**Date:** 2026-08-11

## Scope

R0.7A-1 established additive Architecture v0.2 compatibility primitives without migrating SQLite schema v1 or existing Shot/Asset millisecond fields.

Implemented:

- exact rational `MediaTime` / `MediaTimeRange` primitives;
- `EntityEnvelope.derived_from` exact revision lineage with backward-compatible codec handling;
- typed Asset origin / usage-role policy primitives;
- fail-closed legacy remote/generated visual-origin classification;
- reference-analysis-only visual Resolver exclusion;
- `RightsAttestation`, `LicenseSnapshot`, `ManualLicenseOverride`, rights eligibility state;
- artifact lifecycle classes separating rebuildable cache, durable derived evidence and project outputs;
- audio-only, rights-aware future `AudioMaterialProvider` seam with generated audio default OFF;
- removal of the old generic `MaterialProvider` remote-media seam.

Not changed:

- SQLite `user_version` remains 1;
- Asset/Shot persistent schema remains the R0.4 shape;
- Shot/source-time authoritative field migration is deferred to R0.7A-2;
- no new external dependency/model/provider was introduced;
- no creative Director/Resolver behavior was implemented.

## Quality gate evidence

GitHub Actions run `31452205616` reported:

```text
format: 207 files already formatted
lint: all checks passed
mypy: 104 source files, no issues
pytest: 168 passed
Import Linter: 3 contracts kept, 0 broken
build: source distribution + wheel succeeded
whitespace: passed
```

Commit statuses:

```text
ci/quality-gate-diagnostic = success
ci/sqlite-persistence-windows = success
```

An earlier commit `69ac35b0...` failed only Ruff E501 on one 102-character docstring. The follow-up `22ff30bb...` changed that line only; mypy, pytest, import contracts, build and whitespace were already passing on the failed attempt.

## Architecture evidence

The batch proves that v0.2 policy primitives can enter the current repository without breaking existing R0.1–R0.6 ownership seams or SQLite persistence behavior.

The next source migration is R0.7A-2, split for risk control into:

1. Shot / detector / sampling / understanding source-time migration;
2. Asset duration / ingest media-time migration.

SQLite schema/codec v2 migration remains isolated in R0.7A-3.
