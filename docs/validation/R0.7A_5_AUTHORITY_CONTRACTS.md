# R0.7A-5 Future Authority Contracts — Validation Record

**Status:** COMPLETE  
**Validated commit:** `6bf45ba6fa6a5017f90b844184eeeabe4a6142cb`  
**Date:** 2026-08-11

## Scope

R0.7A-5 established the Architecture v0.2 ownership contracts needed by later creative phases without implementing any Director, Resolver, music-selection, audio-mixing, spatial-composition or Review algorithm.

Implemented contracts:

- `CandidateWindow`, `ResolvedSelection`, `ResolutionDecision`;
- one EditSlot may resolve to multiple ordered selections;
- `TemporalEvidence` / `TemporalAnchor` exact source-time evidence objects;
- `ShotResolver` request/decision ownership seam;
- `SpatialComposer` / `ReframeDecision` source-time spatial intent seam;
- `MusicSelectionService` / `MusicSelectionDecision` source-window seam;
- `AudioEditorialService` / `AudioMixDecision` slot/policy automation-intent seam;
- layered `ReviewStage`, `ReviewFinding` and repair-routing metadata;
- EDL segments with exact rational source and **timeline** ranges;
- deterministic executor contracts using typed argv rather than shell-command strings;
- explicit `UntrustedText` wrapper for OCR/transcript/reference/provider-derived text.

No external model/provider, network call, tracking algorithm, music library, renderer or creative policy was introduced.

## Authority invariants proved

Dedicated tests prove:

```text
Director/Resolver-side decisions
→ may select source windows
→ do not own exact timeline placement

SpatialComposer
→ owns source-time crop/scale intent
→ does not own timeline time

MusicSelectionService
→ owns music Asset/source moments
→ does not own timeline placement

AudioEditorialService
→ owns mix/duck/fade intent against slots/assets
→ does not own exact timeline automation

EDLBuilder/EDL
→ first authority boundary carrying exact timeline ranges
```

Additional tests prove:

- an unresolved ResolutionDecision cannot hide concrete selections;
- source-time temporal anchors remain exact rational values;
- Review findings route work back to the responsible owner and do not replace Shot/EDL decisions themselves;
- media-derived hostile text remains data and cannot become a shell command through the deterministic executor contract;
- NUL-containing tool arguments are rejected.

## Quality gate evidence

GitHub Actions run `31454987721` reported:

```text
format: 228 files already formatted
lint: all checks passed
mypy: 114 source files, no issues
pytest: 201 passed
Import Linter: 3 contracts kept, 0 broken
build: source distribution + wheel succeeded
whitespace: passed
ci/quality-gate-diagnostic = success
```

The initial source commit `128e9585...` had already passed mypy, 201 tests, import contracts, build and whitespace; it failed only Ruff `I001` import ordering in the new authority-contract test file. The final validated commit changes import order only.

## Next phase

`R0.7A-6 — Migration Closure`

Engineering closure must verify the full migration invariants and preserve R0.1–R0.6 probes. A private real-phone-footage Product Probe remains a separate truth claim and must use actual user-owned footage rather than a generated fixture.
